{
  "name": "runtimes-mastrapod-example",
  "version": "0.1.0",
  "description": "Example for MastraPod API",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc && cp src/*.yaml dist/ && cp src/*.js dist/",
    "dev": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "kastra": "file:../../mastra-runtimes-0.1.0.tgz"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
